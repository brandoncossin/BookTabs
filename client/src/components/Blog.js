import { render } from '@testing-library/react';
import React, { useState } from 'react';

const Blog = () =>{
    return(
        <div class="container">
        <p>This is the Blog</p>
        </div>
    );
}
export default Blog;